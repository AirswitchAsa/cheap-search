# Copy this file to .env (gitignored) and fill in real values.
# Required for the live test suite; the binary reads XAI_API_KEY from
# whatever environment you invoke it in.
XAI_API_KEY=xai-replace-me
